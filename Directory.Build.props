<Project>
  <PropertyGroup>
    <VersionPrefix>1.0</VersionPrefix>
    <VersionSuffix Condition="'$(VersionSuffix)' == ''">0-dev</VersionSuffix>
    <Version>$(VersionPrefix).$(VersionSuffix)</Version>
  </PropertyGroup>
</Project>